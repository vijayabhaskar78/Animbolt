# Frontend

Next.js app for prompting, scene iteration, preview watching, and export management.

## Run locally

1. Create env:
   - `cp .env.local.example .env.local`
2. Install:
   - `npm install`
3. Start:
   - `npm run dev`

