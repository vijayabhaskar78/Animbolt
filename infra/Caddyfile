:80 {
    encode gzip

    @api path /api/* /ws/* /health /docs* /openapi.json /artifacts/*
    handle @api {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy web:3000
    }
}

